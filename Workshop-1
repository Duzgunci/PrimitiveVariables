/*string name = 'Sara';
system.debug('My name is: ' +name);
string lastname = 'Maple';
system.debug ('My last name is: ' +lastname);
string age = '34';
system.debug ('I am '+age +' years old');

system.debug('My full name is ' +name  +lastname);
    
string country = 'United States';
system.debug('My country is ' +country);

integer myage = 34;
system.debug ('My age is ' +myage);

string ITschool= '‘welcome to CLARUSWAY Salesforce developer Course’';
system.debug('Lowercase -' +ITschool.toLowerCase());
system.debug('Uppercase +' +ITschool.toUpperCase());
system.debug('Length: ' +ITschool.length());
String name = 'welcome to CLARUSWAY Salesforce developer Course';
List<String> names = name.split(' ');
for (Integer i = 0; i < names.size(); i++)
    names[i] = names[i].capitalize();
name = String.join(names, ' ');
system.debug(name); // yields Welcome to CLARUSWAY Salesforce Developer Course

String str ='CLARUSWAY';
System.debug('Replace value is ' +str.replace( 'C', 'K' ) );
*/

String original = 'Clarusway';
String reversed = original.reverse();
system.debug(reversed);



